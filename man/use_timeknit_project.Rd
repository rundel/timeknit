% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{use_timeknit_project}
\alias{use_timeknit_project}
\alias{remove_timeknit_project}
\title{Set up a project to report chunk times}
\usage{
use_timeknit_project(path = ".")

remove_timeknit_project(path = ".")
}
\arguments{
\item{path}{The project root, or any directory inside it. The root is the
nearest directory at or above \code{path} containing \verb{_quarto.yml}, an
\code{.Rproj} file, \code{DESCRIPTION}, or \code{.git}, falling back to \code{path} itself.}
}
\value{
The project root, invisibly.
}
\description{
Configures a project so that knitr and Quarto renders started from it use
timeknit, without touching \verb{~/.Rprofile} or the site-wide profiles.
}
\details{
\code{use_timeknit_project()} makes these changes at the project root:
\itemize{
\item Adds a managed block to the project's \code{.Rprofile}, creating the file if
needed, that calls \code{\link[=use_timeknit]{use_timeknit()}}. This covers R sessions started in the
project, \code{rmarkdown::render()} and \code{knitr::knit()} from those sessions, and
Quarto renders of documents in the project root. When the file is created
from scratch the block also sources \verb{~/.Rprofile} first, because a project
\code{.Rprofile} otherwise replaces it.
\item In a Quarto project (one with \verb{_quarto.yml}), also writes a
\code{.timeknit.Rprofile} and points \code{R_PROFILE_USER} at it from
\verb{_environment.local}. Quarto starts R in each document's own directory,
where the project \code{.Rprofile} is not read, so this is what reaches
documents in subdirectories. The profile loads whatever profile R would
otherwise have used and then activates timeknit. \verb{_environment.local}
holds an absolute path, so in a git repository it is added to
\code{.gitignore}.
}

Existing \code{R_PROFILE_USER} assignments in \verb{_environment.local} are saved as
comments and restored on removal. The generated profile first sources the
previously configured profile from \verb{_environment.local} or \verb{_environment},
if any. Duplicate assignments are preserved, with the last one taking effect.

\code{remove_timeknit_project()} reverses all of this, leaving other content of
those files untouched and deleting files that end up empty.
}
\seealso{
\code{\link[=sitrep]{sitrep()}} to check the result.
}
