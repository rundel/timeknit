% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitrep.R
\name{sitrep}
\alias{sitrep}
\title{Report timeknit's status for a project}
\usage{
sitrep(path = ".")
}
\arguments{
\item{path}{The project root, or any directory inside it. The root is the
nearest directory at or above \code{path} containing \verb{_quarto.yml}, an
\code{.Rproj} file, \code{DESCRIPTION}, or \code{.git}, falling back to \code{path} itself.}
}
\value{
A list with the collected facts, invisibly. The \code{documents} element
is a data frame with one row per directory holding \code{.qmd} or \code{.Rmd} files.
}
\description{
Prints a situation report: whether timeknit is active in this session and
knitr's progress display is enabled, which profiles activate it for the
project, and which documents in the project are reached when rendered with
Quarto. Nothing is changed.
}
\details{
A document is counted as covered when the profile R will read for it
activates timeknit. Quarto starts R in the document's directory, so that
profile is \code{R_PROFILE_USER} if set (in this session's environment or in the
project's \verb{_environment.local} or \verb{_environment}), otherwise a \code{.Rprofile} in
the document's directory, otherwise \verb{~/.Rprofile}.
}
\seealso{
\code{\link[=use_timeknit_project]{use_timeknit_project()}} to set a project up.
}
