% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress.R
\name{knit_progress}
\alias{knit_progress}
\title{A knitr progress display that reports chunk execution times}
\usage{
knit_progress(total, labels)
}
\arguments{
\item{total}{The number of blocks (text and code) in the document.}

\item{labels}{A character vector of chunk labels, with \code{""} for text blocks.}
}
\value{
A list of functions \code{update(i)}, \code{interrupt()}, and \code{done()}.
}
\description{
A drop-in replacement for knitr's default progress display. Each block of the
document is printed as \code{i/total [label]}, exactly as knitr does, and once a
code chunk finishes its elapsed wall clock time is appended to that line
using an appropriate unit (see \code{\link[=format_duration]{format_duration()}}). When the document
finishes, a short summary reports the total chunk time and the slowest
chunks.
}
\details{
knitr calls this function itself with the number of blocks and their labels,
then calls the returned \code{update()}, \code{interrupt()}, and \code{done()} functions as
it works through the document. Register it with knitr through the
\code{knitr.progress.fun} option, most conveniently by calling \code{\link[=use_timeknit]{use_timeknit()}}
from your \code{.Rprofile}.

Child documents get their own nested display: their lines are indented, they
print no summary, and the parent chunk's line is repeated with its time once
the child finishes. When a chunk errors, its time is written to the message
stream so it still appears before the error.
}
\section{Options}{

\itemize{
\item \code{timeknit.slowest}: how many of the slowest chunks to list in the summary.
Defaults to 5, and \code{0} lists none.
\item \code{timeknit.summary}: whether to print the summary at all. Defaults to
\code{TRUE}.
\item \code{timeknit.text_blocks}: whether to print lines for text (non-chunk)
blocks as knitr does by default. Defaults to \code{TRUE}.
\item \code{knitr.progress.output}: knitr's own option for where progress output is
written. Defaults to the console.
}
}

\examples{
pb = knit_progress(3, c("", "setup", "model"))
pb$update(1)
pb$update(2)
pb$update(3)
pb$done()
}
