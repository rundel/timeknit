% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/record.R
\name{timeknit-record}
\alias{timeknit-record}
\title{Timing records for editor integrations}
\description{
When a document is knitted from a file, \code{\link[=knit_progress]{knit_progress()}} also writes a JSON
record of the chunk timings so that editor extensions can show them inline.
The record goes to \verb{<root>/.quarto/timeknit/<relative path>.json}, where
\code{root} is the Quarto project directory (\code{QUARTO_PROJECT_DIR}, which Quarto
sets even for single-file renders) or, outside Quarto, the document's own
directory. Quarto projects already ignore \verb{.quarto/} in git.
}
\details{
The record is a JSON object with \code{file} (absolute document path), \code{root},
\code{relative}, \code{time} (UTC), \code{status} (\code{"complete"} or \code{"error"}), \code{failed}
(the label of the chunk that errored, or \code{null}), \code{total} (seconds), and
\code{chunks}, an array with one object per executed chunk holding \code{label},
\code{start} and \code{end} (1-based source lines of the chunk fences), \code{elapsed}
(seconds), and \code{code} (the chunk body without leading \verb{#|} option lines).
Child documents are not recorded separately; their time is part of the
parent chunk.

Control it with the \code{timeknit.record} option: \code{TRUE} (the default) writes to
the location above, \code{FALSE} disables recording, and a directory path writes
\verb{<relative path>.json} below that directory instead.
}
\keyword{internal}
